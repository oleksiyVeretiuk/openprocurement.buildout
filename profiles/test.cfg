[buildout]
parts +=
    test_scripts

[test_scripts]
recipe = zc.recipe.egg
interpreter = python
eggs =
    pytest-mock
    pytest-cov
    pytest
    nose
    WebTest
    coverage
    mock

scripts =
    nosetests
    pytest
